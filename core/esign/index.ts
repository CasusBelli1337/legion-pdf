/**
 * The core/esign surface the IPC layer calls. Pure over bytes, Node-safe,
 * OpResult counts verified against the saved output — same contract as
 * core/ops.
 */

export { buildFillableForm } from './fillable-form';
