/**
 * Left rail slot. The viewer lane owns what goes in it: page thumbnails and
 * the document's bookmarks (src/components/thumbnails/**).
 */

import { DocumentRail } from '../../components/thumbnails';

export function ThumbnailRail() {
  return <DocumentRail />;
}
